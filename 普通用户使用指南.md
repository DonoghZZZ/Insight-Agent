# Insight Agent 普通用户使用指南

这份指南写给不熟悉代码的用户。你不需要理解项目源码，只要按下面步骤启动即可。

## 你会得到什么

Insight Agent 是一个本地运行的 AI 数据分析工作台，可以用于：

- 上传 CSV、Excel、JSON 数据文件并做基础分析。
- 采集公开平台数据，再生成分析报告。
- 配置 DeepSeek API Key 后，与 AI 数据分析师对话追问。
- 生成本地 HTML 分析报告。

## 第一次使用

### macOS 用户

1. 下载并解压项目。
2. 打开项目文件夹。
3. 优先双击 `Insight Agent.app`。
4. 如果系统提示无法打开，请右键点击它，选择“打开”。
5. 也可以双击 `启动 Insight Agent.command` 作为备用入口。
6. 图形化启动器会检查环境，并自动打开浏览器工作台。

### Windows 用户

1. 下载并解压项目。
2. 打开项目文件夹。
3. 双击 `启动 Insight Agent.bat`。
4. 启动器会检查环境，并自动打开浏览器工作台。

## 需要提前安装什么

电脑需要安装 Python 3.9 或更高版本。

如果没有安装 Python，启动器会提示你安装。下载地址：

https://www.python.org/downloads/

## 启动器会自动做什么

双击启动后，启动器会自动检查：

- Python 版本是否合适。
- 项目依赖是否安装。
- `.env` 配置文件是否存在。
- 是否配置了 DeepSeek API Key。
- 是否需要安装 Playwright Chromium 浏览器组件。
- 本地 Web 工作台是否能启动。

如果缺少依赖，启动器会自动安装并显示进度日志。

## DeepSeek API Key 是什么

AI 对话、主题提取、观点挖掘等功能需要 DeepSeek API Key。

没有 Key 也可以先使用：

- 上传文件。
- 基础统计。
- 词频分析。
- 部分本地分析功能。

如果你有 DeepSeek API Key，可以在启动器提示时粘贴。启动器会保存到 `.env` 文件里。

## 打开后怎么用

浏览器打开后，你会看到 Insight Agent 工作台。

推荐流程：

1. 点击“上传/分析文件”，上传 CSV、Excel 或 JSON。
2. 进入“分析”页面，选择一个分析模板。
3. 等待系统生成结果和报告。
4. 如果已配置 API Key，可以进入“AI 对话”，继续追问数据细节。

如果想采集新数据：

1. 点击“开始采集数据”。
2. 选择平台，或者用自然语言描述需求。
3. 等采集完成后进入分析页面。

## 注意事项

- 启动后请不要关闭启动器窗口，关闭后本地服务会停止。
- 第一次安装依赖可能需要几分钟。
- 爬虫功能可能需要平台登录，也可能受平台页面变化影响。
- 报告文件保存在 `output/reports/` 目录。
- 数据文件保存在 `output/data/` 目录。

## 常见问题

### 双击后没有打开浏览器

请看启动器窗口里显示的地址，通常是：

http://127.0.0.1:9527

复制到浏览器打开即可。

### 提示缺少依赖

选择自动安装。如果安装失败，通常是网络问题，可以换网络后重新双击启动。

### AI 对话不能用

请确认已经配置 DeepSeek API Key。没有 Key 时，AI 对话和定性分析不可用。

### 采集数据失败

公开平台页面经常变化，爬虫可能需要更新。可以先用“上传文件”体验分析流程。
